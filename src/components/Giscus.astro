---
export interface Props {
  slug: string;
  shopName: string;
}

const { slug, shopName } = Astro.props;
const discussionUrl = `https://github.com/clawdiard/boardroom-nyc/discussions`;
---

<section class="reviews-section">
  <div class="reviews-header">
    <h2>Community Reviews</h2>
    <p class="reviews-guidelines">
      Share your experience at {shopName}! Please keep reviews respectful and helpful.
      Rate the shop, share what you bought, or describe the vibe.
    </p>
    <div class="guidelines-box">
      <h4>📋 Community Guidelines</h4>
      <ul>
        <li>Be respectful and constructive</li>
        <li>Share honest, first-hand experiences</li>
        <li>No spam, self-promotion, or hate speech</li>
        <li>Support local — we're all in this together 🛹</li>
      </ul>
    </div>
  </div>

  <div class="reviews-cta">
    <a href={discussionUrl} target="_blank" rel="noopener noreferrer" class="reviews-button">
      💬 Join the Discussion on GitHub
    </a>
    <p class="reviews-note">
      Leave a review or read what others are saying about {shopName} in our GitHub Discussions.
    </p>
  </div>
</section>

<style>
  .reviews-section {
    margin-top: var(--space-8);
    padding-top: var(--space-6);
    border-top: 2px solid var(--color-gray-700);
  }

  .reviews-header {
    margin-bottom: var(--space-5);
  }

  .reviews-header h2 {
    font-family: var(--font-display);
    font-size: var(--text-lg);
    text-transform: uppercase;
    margin: 0 0 var(--space-3);
    color: var(--text-primary);
  }

  .reviews-guidelines {
    font-size: var(--text-sm);
    color: var(--text-secondary);
    line-height: 1.6;
    margin: 0 0 var(--space-4);
  }

  .guidelines-box {
    background: var(--surface-card);
    border: 1px solid var(--color-gray-700);
    padding: var(--space-3) var(--space-4);
    font-size: var(--text-sm);
  }

  .guidelines-box h4 {
    font-family: var(--font-display);
    font-size: var(--text-sm);
    text-transform: uppercase;
    margin: 0 0 var(--space-2);
    color: var(--color-crimson);
  }

  .guidelines-box ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .guidelines-box li {
    color: var(--text-muted);
    padding: var(--space-1) 0;
  }

  .guidelines-box li::before {
    content: '→ ';
    color: var(--color-crimson);
  }

  .reviews-cta {
    text-align: center;
    padding: var(--space-6) var(--space-4);
    background: var(--surface-card);
    border: 1px solid var(--color-gray-700);
  }

  .reviews-button {
    display: inline-block;
    padding: var(--space-3) var(--space-5);
    background: var(--color-crimson);
    color: white;
    font-family: var(--font-display);
    font-size: var(--text-base);
    text-transform: uppercase;
    text-decoration: none;
    letter-spacing: 0.05em;
    transition: background 0.2s ease;
  }

  .reviews-button:hover {
    background: #b91c1c;
  }

  .reviews-note {
    margin-top: var(--space-3);
    font-size: var(--text-sm);
    color: var(--text-muted);
  }
</style>

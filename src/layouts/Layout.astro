---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="Board Room NYC — Your guide to NYC's skateboard scene" />
    <link rel="icon" type="image/svg+xml" href="/boardroom-nyc/favicon.svg" />
    <link rel="manifest" href="/boardroom-nyc/manifest.json" />
    <meta name="theme-color" content="#1a1a2e" />
    <title>{title}</title>
    <script defer data-domain="clawdiard.github.io/boardroom-nyc" src="https://plausible.io/js/script.js"></script>
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  :root {
    --accent: #e94560;
    --bg: #1a1a2e;
    --text: #eee;
  }
  html {
    font-family: system-ui, sans-serif;
    background: var(--bg);
    color: var(--text);
  }
  body {
    margin: 0;
    padding: 2rem;
    min-height: 100vh;
  }
  h1 { color: var(--accent); }
  a { color: var(--accent); }
</style>
